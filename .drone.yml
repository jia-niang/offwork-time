kind: pipeline
name: default

workspace:
  path: /home/node/app

steps:
  - name: build
    image: node:14
    volumes:
      - name: yarncache
        path: /home/node/.cache/yarn
    commands:
      - yarn --registry=https://registry.yarnpkg.com
      - yarn build
    environment:
      CI: false
      GENERATE_SOURCEMAP: false

  - name: deploy
    image: node:14
    volumes:
      - name: webroot
        path: /webroot
    commands:
      - mkdir -p /webroot/offwork/
      - rm -rf /webroot/offwork/*
      - cp -rf ./build/* /webroot/offwork/

volumes:
  - name: webroot
    host:
      path: /root/www
  - name: yarncache
    host:
      path: /root/.yarncache

trigger:
  branch:
    - master
